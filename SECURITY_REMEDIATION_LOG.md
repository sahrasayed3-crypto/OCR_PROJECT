# سجل المعالجة الأمنية

- `7058238`: توثيق النطاق، الأصول، حدود الثقة، STRIDE وحالات الإساءة قبل التعديل.
- `4dad75c`: إصلاح حدود الملفات، الأرشيف، SSRF، OCR، النموذج، التدريب، SQL، API، النشر وCI مع اختبارات استغلال.
- تحديث لاحق لسلسلة التوريد: setuptools 83، extras أمنية، Hypothesis وSBOM.
- لكل نتيجة مؤكدة اختبار فشل قبل الإصلاح ثم نجاح بعده؛ الربط الكامل في `SECURITY_FINDINGS.json`.
- لم تُحذف وظيفة عاملة، ولم تُعطل حماية لتنجح الاختبارات.
- لم يحدث push أو PR، ولم تُستخدم بيانات أو أسرار حقيقية.
