# Security

- Do not commit credentials.
- Do not store AWS keys in config files.
- Keep source data, generated datasets, model weights, cache, temp files, and logs out of Git when they become large or sensitive.
- Keep all automated writes inside the project directory unless explicitly approved.
- Treat downloaded documents and licenses as future reviewed ingestion inputs, not foundation assets.

