# تقرير تنفيذ التشويه

- السجل: 108 أسماء؛ 107 مشغلات بكسلية حقيقية و`metadata_only` واحد للاختبار فقط.
- الواجهة المشتركة: `DistortionOperator.apply(image, seed)`.
- الحتمية: نفس seed يعيد نفس البكسلات والبيانات الوصفية؛ كل المشغلات الـ107 نُفذت في الاختبار.
- ملفات التعريف: 18 YAML إلزامية، JSON Schema، حد طول سلسلة وحد ميزانية شدة ومشغلات مسجلة فقط.
- manifest: seed، السلسلة، المعاملات، الشدة، المناطق، الأبعاد، checksums، الترخيص والحالة.
- قبول 20260723T205210Z: 30/30 مخرجًا مختلفًا عن المصدر.
- التخطيط: يستخدم المناطق عند وجودها، ويسجل `whole_page_fallback` عند غيابها.

المصدر لا يُكتب فوقه، والمخرجات محصورة في dataset state root.
